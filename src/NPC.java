public class NPC {
    private TagList tags;
    private Controller controller;

    public NPC(TagList tags, Controller controller){
        this.tags = tags;
        this. controller = controller;
    }
}
